<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/main"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:padding="@dimen/game_screen_padding"
  android:clipChildren="false"
  android:clipToPadding="false"
  android:background="@color/color_content_bg">

  <TextView
    android:id="@+id/elapsed_time"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="@id/target"
    app:layout_constraintEnd_toStartOf="@id/target"
    app:layout_constraintBottom_toBottomOf="@id/target"
    style="@style/Theme.Summy.DefaultTextAppearance"
    tools:text="12:23"/>

  <TextView
    android:id="@+id/target"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="@id/target"
    style="@style/Theme.Summy.TargetStyle"
    tools:text="89"/>

  <TextView
    android:id="@+id/move_counter"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toEndOf="@id/target"
    app:layout_constraintBottom_toBottomOf="@id/target"
    android:gravity="end"
    style="@style/Theme.Summy.DefaultTextAppearance"
    tools:text="5"/>

  <TextView
    android:id="@+id/sum"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@id/target"
    android:layout_marginTop="@dimen/sum_top_spacing"
    style="@style/Theme.Summy.SumStyle"
    tools:text="28"/>

  <LinearLayout
    android:id="@+id/addends_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_marginBottom="@dimen/addend_bottom_spacing"
    app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>